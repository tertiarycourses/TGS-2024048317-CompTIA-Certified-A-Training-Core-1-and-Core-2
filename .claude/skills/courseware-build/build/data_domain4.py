"""Domain 4 — Virtualization and Cloud Computing (Core 1, 11%). Labs 19–21."""

DOMAIN4 = [
    dict(
        num=19, topic=4,
        title="Hypervisor Types and Virtual Machine Resource Planning",
        objective="Distinguish Type 1 from Type 2 hypervisors, plan virtual machine resource allocation and identify the firmware prerequisite for virtualization (Core 1 objective 4.2).",
        desc="You compare the two hypervisor types on where they run and what they are for, verify that hardware virtualization support is enabled, then plan resource allocation for several VMs on one host — the calculation that decides whether a host performs or crawls.",
        build="A hypervisor comparison table, a verified virtualization-enabled host and a resource allocation plan for four VMs.",
        services="Windows host, Task Manager, systeminfo, BIOS/UEFI, VirtualBox or Hyper-V, hypervisor references",
        steps=[
            ("Build the comparison table with rows for Type 1 and Type 2 and columns for where it runs, examples, typical use, performance and management overhead.", ""),
            ("Record that a Type 1 bare-metal hypervisor such as VMware ESXi, Microsoft Hyper-V Server or Citrix Hypervisor runs directly on hardware with no host OS beneath it.", ""),
            ("Record that a Type 2 hosted hypervisor such as VirtualBox, VMware Workstation or Parallels runs as an application on top of a desktop operating system.", ""),
            ("Explain why Type 1 performs better — there is no host operating system competing for CPU and memory or adding a scheduling layer.", ""),
            ("Check whether hardware virtualization support is enabled on your host, since no 64-bit guest will start without it.", "systeminfo | findstr /C:\"Hyper-V\""),
            ("Confirm the same from Task Manager by opening Performance then CPU and reading the Virtualization field.", "taskmgr"),
            ("If virtualization is disabled, record the exact BIOS/UEFI menu path where Intel VT-x or AMD-V is enabled, and note that this is the single most common cause of a VM refusing to start.", ""),
            ("Record the host's total physical resources: CPU cores and logical processors, total RAM, and free disk space.", "systeminfo | findstr /C:\"Total Physical Memory\" /C:\"Processor\""),
            ("Plan four VMs — a Windows desktop, a Linux server, a Windows server and a test machine — assigning vCPU, RAM and disk to each based on the guest OS minimum plus a working margin.", ""),
            ("Sum the planned RAM across all four VMs and confirm the total leaves at least 4 GB for the host, since over-committing RAM is the fastest way to bring a host to a halt.", ""),
            ("Explain the difference between a thick-provisioned disk, which claims all its space immediately, and a thin-provisioned disk, which grows as it fills, and state the risk of over-committing thin disks.", ""),
            ("Explain what a snapshot does, why it is not a backup, and why leaving snapshots in place for weeks degrades performance and consumes disk.", ""),
        ],
        flow=[
            ("Separate the types", "Type 1 on bare metal, Type 2 on a host OS"),
            ("Explain the overhead", "No host OS means no competing scheduler"),
            ("Check the firmware", "VT-x or AMD-V must be on or no 64-bit guest starts"),
            ("Measure the host", "Cores, total RAM and free disk before allocating"),
            ("Allocate and check", "Leave at least 4 GB for the host after all four VMs"),
        ],
        test="Virtualization support is confirmed enabled on the host, your allocation plan's total RAM leaves at least 4 GB for the host, and every VM is allocated at or above its guest OS minimum with the margin stated.",
    ),
    dict(
        num=20, topic=4,
        title="Building and Configuring a Linux Virtual Machine",
        objective="Provision, configure and verify a Linux virtual machine, and perform the post-installation tasks a technician must complete on any new guest (Core 1 objective 4.2).",
        desc="You provision a working Linux environment and complete the full post-installation checklist — updates, networking, users, storage and a snapshot — using the Killercoda Ubuntu playground so that no local install or licence is required and every learner gets an identical environment.",
        build="A configured Linux environment with verified networking, a created user, installed packages and a documented build record.",
        services="Killercoda Ubuntu Playground (https://killercoda.com/playgrounds/scenario/ubuntu)",
        steps=[
            ("Open https://killercoda.com/playgrounds/scenario/ubuntu and wait for the terminal to become available. This is a real Ubuntu machine with root access, running in your browser.", ""),
            ("Record the guest's identity: distribution, version, kernel and architecture — the first thing to establish on any unfamiliar machine.", "cat /etc/os-release && uname -a"),
            ("Record the allocated resources: CPU count, total and available memory, and disk capacity and usage.", "nproc && free -h && df -h /"),
            ("Update the package index and apply available upgrades, the mandatory first post-installation task on any new guest.", "apt-get update -qq && apt-get upgrade -y -qq"),
            ("Install the tool set this course uses across the Linux labs.", "apt-get install -y net-tools iproute2 dnsutils curl vim tree htop"),
            ("Record the network configuration: interface addresses, the default route and the configured DNS resolvers.", "ip -brief addr show && ip route show && cat /etc/resolv.conf"),
            ("Verify outbound connectivity and DNS resolution in one test, then record the result.", "ping -c 3 8.8.8.8 && dig +short www.tertiarycourses.com.sg"),
            ("Create a standard non-root user for daily work, following the principle of least privilege.", "useradd -m -s /bin/bash aplus && echo 'aplus:Training2026!' | chpasswd"),
            ("Grant that user administrative rights through sudo rather than by using the root account directly.", "usermod -aG sudo aplus && groups aplus"),
            ("Create a working directory structure for the course labs and confirm it was created as expected.", "mkdir -p /home/aplus/labs/{core1,core2,evidence} && tree /home/aplus"),
            ("Set correct ownership on the new directories so the standard user, not root, owns their own files.", "chown -R aplus:aplus /home/aplus/labs && ls -ld /home/aplus/labs"),
            ("Write the build record capturing the OS version, resources, installed packages, created user and network settings, so the machine could be rebuilt identically.", "echo \"Built $(date) on $(cat /etc/os-release | grep PRETTY | cut -d'\\\"' -f2)\" > /home/aplus/labs/build-record.txt && cat /home/aplus/labs/build-record.txt"),
        ],
        flow=[
            ("Open the playground", "A real Ubuntu machine with root, in a browser tab"),
            ("Identify the guest", "Distribution, version, kernel and architecture"),
            ("Patch and install", "Update the index, upgrade, then add the tool set"),
            ("Verify the network", "Addresses, default route, resolvers, ping and dig"),
            ("Create a user", "Standard account plus sudo, owning its own directories"),
        ],
        test="The OS version and resources are recorded, updates complete without error, ping and dig both succeed, the aplus user exists in the sudo group, and the labs directory tree is owned by aplus rather than root.",
    ),
    dict(
        num=21, topic=4,
        title="Cloud Service and Deployment Model Selection",
        objective="Compare IaaS, PaaS and SaaS and the public, private, community and hybrid deployment models, and select the correct combination for a stated business requirement (Core 1 objective 4.1).",
        desc="You build the responsibility matrix that separates the three service models, compare the four deployment models on the concerns that decide them, and then solve selection scenarios where the wrong choice creates either a compliance breach or an unnecessary cost.",
        build="A shared-responsibility matrix, a deployment model comparison and six justified service and deployment selections.",
        services="Cloud provider documentation, IaaS/PaaS/SaaS reference material, organisational scenario briefs",
        steps=[
            ("Build the shared-responsibility matrix with rows for networking, storage, servers, virtualization, operating system, middleware, runtime, data and applications.", ""),
            ("Mark for on-premises that the customer manages every layer, which is the baseline the three cloud models are measured against.", ""),
            ("Mark for IaaS that the provider manages up to virtualization and the customer manages the operating system and everything above it — examples are AWS EC2, Azure Virtual Machines and Google Compute Engine.", ""),
            ("Mark for PaaS that the provider manages up to the runtime and the customer manages only applications and data — examples are Azure App Service, Google App Engine and Heroku.", ""),
            ("Mark for SaaS that the provider manages every layer and the customer manages only their own data and users — examples are Microsoft 365, Google Workspace and Salesforce.", ""),
            ("Compare the four deployment models on cost, control, security, compliance and typical use: public, private, community and hybrid.", ""),
            ("Record the five cloud characteristics that define the model: shared resources, rapid elasticity, high availability, file synchronisation and metered utilisation.", ""),
            ("Explain metered utilisation and why it changes budgeting — you pay for what you consume, so an idle over-provisioned resource is pure waste.", ""),
            ("Solve the scenario of a hospital storing patient records under strict data residency rules, and justify why private cloud is correct and public is not.", ""),
            ("Solve the scenario of a startup needing email and document collaboration with no IT staff, and justify why SaaS on public cloud is correct.", ""),
            ("Solve the scenario of a retailer with steady baseline load and extreme seasonal peaks, and justify why hybrid cloud with cloud bursting is correct.", ""),
            ("Solve the remaining scenarios — several hospitals sharing a compliance platform, a development team needing full OS control, and a team deploying code without managing servers — and justify each.", ""),
        ],
        flow=[
            ("Build the matrix", "Nine layers across on-premises, IaaS, PaaS and SaaS"),
            ("Place the boundary", "Who manages the OS, the runtime and the application"),
            ("Compare deployments", "Public, private, community and hybrid on control and cost"),
            ("Recall the traits", "Shared, elastic, available, synchronised and metered"),
            ("Select per scenario", "Name both a service and a deployment model, and justify"),
        ],
        test="Your responsibility matrix correctly divides all nine layers across on-premises, IaaS, PaaS and SaaS; every scenario names both a service model and a deployment model; and each justification cites the specific requirement that ruled the alternatives out.",
    ),
]
