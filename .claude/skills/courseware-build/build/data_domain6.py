"""Domain 6 — Operating Systems (Core 2, 28%). Labs 29–36."""

DOMAIN6 = [
    dict(
        num=29, topic=6,
        title="Windows Editions, Installation Types and Partitioning",
        objective="Select the correct Windows edition and installation type for a requirement, and plan MBR or GPT partitioning to match the firmware mode (Core 2 objectives 1.1 and 1.9).",
        desc="You match Windows editions to requirements, choose the right installation type for each situation, and plan the partition scheme — the decision that determines whether the installer will even proceed, since UEFI Secure Boot requires GPT.",
        build="An edition selection matrix, four justified installation-type selections and a partition plan matched to the firmware mode.",
        services="Windows installation media or reference, Disk Management, diskpart, msinfo32",
        steps=[
            ("Build the edition matrix with rows for Home, Pro, Pro for Workstations and Enterprise, and columns for domain join, BitLocker, Group Policy, Remote Desktop host and maximum RAM.", ""),
            ("Record the decisive difference: Home cannot join a domain and has no BitLocker or Group Policy, which rules it out for almost every business deployment.", ""),
            ("Compare the installation types — clean install, in-place upgrade, network deployment and cloning — on what each preserves, what it requires and how long it takes per machine.", ""),
            ("Record the valid upgrade paths: Windows 7 and 8.1 upgrade in place to Windows 10, and Windows 10 upgrades in place to Windows 11 where the hardware requirements are met.", ""),
            ("Record the Windows 11 hardware requirements that block many upgrades: TPM 2.0, UEFI with Secure Boot, a 64-bit supported processor, 4 GB RAM and 64 GB storage.", ""),
            ("Record the pre-upgrade checklist: back up files and preferences, verify application and driver compatibility, verify hardware compatibility, and confirm the rollback window.", ""),
            ("Compare MBR and GPT on maximum disk size, partition count and firmware requirement, and record that UEFI Secure Boot requires GPT while legacy BIOS uses MBR.", ""),
            ("Confirm your own machine's firmware mode and Secure Boot state, since this determines which partition scheme is valid.", "msinfo32"),
            ("Open Disk Management and record each disk's partition style, its volumes, their file systems and their free space.", "diskmgmt.msc"),
            ("Inspect the same information from the command line, which is what you use when Windows will not boot into the GUI.", "diskpart /? && echo list disk | diskpart"),
            ("Distinguish primary, extended and logical partitions under MBR, and record why the four-primary limit forces an extended partition when more volumes are needed.", ""),
            ("Compare a full format, which checks every sector for bad sectors, against a quick format, which only rewrites the file system table, and state when each is appropriate.", ""),
        ],
        flow=[
            ("Match the edition", "Home cannot join a domain or run BitLocker"),
            ("Choose the install", "Clean, in-place upgrade, network deploy or clone"),
            ("Check the requirements", "TPM 2.0, UEFI and Secure Boot for Windows 11"),
            ("Pick the partition style", "UEFI Secure Boot needs GPT; legacy BIOS uses MBR"),
            ("Confirm the firmware", "msinfo32 states the BIOS mode and Secure Boot state"),
        ],
        test="Your edition matrix correctly identifies which editions support domain join and BitLocker, each installation-type selection is justified against its requirement, and the partition plan matches the firmware mode confirmed in msinfo32.",
    ),
    dict(
        num=30, topic=6,
        title="Windows Command Line — Navigation, Files and Copy Operations",
        objective="Use the Windows command line for navigation, file management and resilient copy operations, choosing correctly between copy, xcopy and robocopy (Core 2 objective 1.2).",
        desc="You work the Windows command line the way a technician does during a migration or a repair, when the GUI is unavailable or too slow. The lab ends on robocopy, because it is the tool that actually survives a large real-world file migration.",
        build="A command reference sheet and a completed file migration performed with robocopy, verified against the source.",
        services="Windows PC, Command Prompt, PowerShell",
        steps=[
            ("Open Command Prompt as administrator and confirm your starting location and the commands available.", "cd && help | more"),
            ("Practise navigation: move between directories, move up one level, and return to the drive root.", "cd C:\\Users && cd .. && cd \\"),
            ("List directory contents with the switches that matter: all files including hidden, and a recursive listing.", "dir /a && dir /s /b C:\\Windows\\System32\\*.msc"),
            ("Create a directory structure for the exercise and confirm it exists.", "md C:\\aplus-lab\\source && md C:\\aplus-lab\\dest && dir C:\\aplus-lab"),
            ("Create test files with content so the copy operations have something real to move.", "echo Test file one > C:\\aplus-lab\\source\\file1.txt && echo Test file two > C:\\aplus-lab\\source\\file2.txt"),
            ("Use copy for a single file and record its limitation — it copies files only and cannot handle directory trees.", "copy C:\\aplus-lab\\source\\file1.txt C:\\aplus-lab\\dest\\ && dir C:\\aplus-lab\\dest"),
            ("Use xcopy with the switches for subdirectories including empty ones, and record that it handles trees but cannot resume.", "xcopy C:\\aplus-lab\\source C:\\aplus-lab\\dest /E /I /Y"),
            ("Use robocopy to mirror the source to the destination, which is the correct tool for a real migration.", "robocopy C:\\aplus-lab\\source C:\\aplus-lab\\dest /MIR /R:2 /W:2 /LOG:C:\\aplus-lab\\copy.log"),
            ("Read the robocopy log and record the files copied, skipped and failed, and the total bytes transferred.", "type C:\\aplus-lab\\copy.log"),
            ("Record why robocopy is the right choice for migrations: it retries failed files, resumes interrupted transfers, preserves attributes and timestamps, and logs everything.", ""),
            ("Warn on the /MIR switch: it makes the destination identical to the source, which means it deletes files in the destination that are not in the source. Verify the destination before every mirror.", ""),
            ("Verify the migration by comparing both directories, then clean up the exercise files.", "dir C:\\aplus-lab\\source /b && dir C:\\aplus-lab\\dest /b && rd /s /q C:\\aplus-lab"),
        ],
        flow=[
            ("Open an elevated prompt", "Confirm where you are and what is available"),
            ("Navigate and list", "cd, dir with hidden files, and a recursive listing"),
            ("Copy one file", "copy handles files only — no directory trees"),
            ("Copy a tree", "xcopy carries subdirectories but cannot resume"),
            ("Mirror with robocopy", "Retries, resumes and logs — and /MIR deletes"),
        ],
        test="Every command executes without error, the robocopy log confirms the expected file count copied, your reference sheet states the specific limitation of copy and xcopy that robocopy overcomes, and the /MIR deletion warning is recorded.",
    ),
    dict(
        num=31, topic=6,
        title="Windows Networking and Repair Commands",
        objective="Use Windows networking and repair commands to diagnose connectivity and repair system file corruption (Core 2 objective 1.2).",
        desc="You run the diagnostic and repair commands that appear directly in the Core 2 objectives, interpreting the output of each rather than merely executing it — because in support work the output is the diagnosis.",
        build="A command output interpretation record and a completed system file integrity check with results explained.",
        services="Windows PC, Command Prompt as administrator, PowerShell",
        steps=[
            ("Display the full network configuration and record the IP address, subnet mask, default gateway, DNS servers, DHCP server and MAC address.", "ipconfig /all"),
            ("Release and renew the DHCP lease, recording the address before and after to confirm the DHCP server responded.", "ipconfig /release && ipconfig /renew"),
            ("Display and then clear the DNS resolver cache, and record when clearing it is the correct fix — after a DNS record changes but the old answer is still cached.", "ipconfig /displaydns | more && ipconfig /flushdns"),
            ("Test the loopback address first to confirm the TCP/IP stack itself is working before testing anything external.", "ping 127.0.0.1"),
            ("Test the default gateway to separate a local network fault from an internet fault.", "ping -n 4 %GATEWAY%"),
            ("Test an external IP address and then an external name, and record that success on the IP but failure on the name isolates the fault to DNS.", "ping -n 4 8.8.8.8 && ping -n 4 www.tertiarycourses.com.sg"),
            ("Trace the route to a destination and identify where latency increases sharply or where the path stops.", "tracert -h 12 www.tertiarycourses.com.sg"),
            ("Query DNS directly and record the resolver used and the addresses returned.", "nslookup www.tertiarycourses.com.sg"),
            ("List active connections with the owning process ID so you can identify what is actually using the network.", "netstat -ano | findstr ESTABLISHED"),
            ("Run the system file checker to scan and repair protected system files, and record the result it reports.", "sfc /scannow"),
            ("Repair the component store that sfc itself depends on, and record why DISM must be run first when sfc reports it cannot fix the files.", "DISM /Online /Cleanup-Image /RestoreHealth"),
            ("Check the disk for file system errors, and record the difference between /f which fixes errors and /r which also locates bad sectors and takes far longer.", "chkdsk C: /scan"),
        ],
        flow=[
            ("Read the configuration", "ipconfig /all gives address, gateway, DNS and MAC"),
            ("Renew the lease", "Release and renew to prove the DHCP server answers"),
            ("Test in order", "Loopback, gateway, external IP, then external name"),
            ("Isolate DNS", "IP works but name fails means DNS and nothing else"),
            ("Repair the system", "sfc /scannow, then DISM when sfc cannot repair"),
        ],
        test="Every command produces output you have interpreted in writing; ipconfig /all output identifies all six required fields; you correctly state that ping to IP succeeding while ping to name fails means DNS; and the sfc result is recorded with its meaning.",
    ),
    dict(
        num=32, topic=6,
        title="Windows Management Tools — Task Manager, MMC and Snap-ins",
        objective="Use Task Manager, the Microsoft Management Console and its snap-ins to diagnose performance, inspect logs and manage devices, disks, users and tasks (Core 2 objectives 1.3 and 1.4).",
        desc="You work through every Windows administrative tool named in the Core 2 objectives, launching each by its executable name — because in an exam and on a locked-down machine you need the name, not the Start menu path.",
        build="A tool reference table with the launch command and primary use for each, plus a performance triage record from Task Manager.",
        services="Windows PC, Task Manager, MMC snap-ins, Control Panel, Windows Settings",
        steps=[
            ("Open Task Manager and record what each tab tells you: Processes, Performance, App history, Startup, Users, Details and Services.", "taskmgr"),
            ("Perform a performance triage: sort Processes by CPU, then by memory, then by disk, and record the top consumer in each category.", ""),
            ("Open the Startup tab, record every high-impact startup item, and state which you would disable to improve boot time and why.", ""),
            ("Open Event Viewer and examine the System and Application logs, filtering to errors and critical events from the last 24 hours.", "eventvwr.msc"),
            ("Record the three main Windows log categories and what each contains: System for operating system events, Security for audit events, and Application for software events.", ""),
            ("Open Disk Management and record each volume with its file system, capacity, free space and status.", "diskmgmt.msc"),
            ("Open Device Manager and identify any device showing a warning triangle or an unknown device, and record how you would resolve it.", "devmgmt.msc"),
            ("Open Task Scheduler and examine a scheduled task's trigger, action and conditions, then record how you would create a nightly backup task.", "taskschd.msc"),
            ("Open Local Users and Groups, list the local accounts and their group memberships, and record which accounts hold administrative rights.", "lusrmgr.msc"),
            ("Open Performance Monitor, add a counter for processor time and available memory, and record the values observed over one minute.", "perfmon.msc"),
            ("Open System Configuration and record what each tab controls, then explain when a diagnostic startup is the correct troubleshooting step.", "msconfig"),
            ("Build the reference table listing every tool with its launch command and its primary use, and add cleanmgr, dfrgui, regedit and resmon to complete it.", "cleanmgr && dfrgui && resmon"),
        ],
        flow=[
            ("Triage in Task Manager", "Sort by CPU, then memory, then disk"),
            ("Trim the startup", "Disable the high-impact items that slow the boot"),
            ("Read the logs", "Event Viewer, filtered to errors in the last 24 hours"),
            ("Open the snap-ins", "diskmgmt, devmgmt, taskschd, lusrmgr and perfmon"),
            ("Complete the toolkit", "msconfig, cleanmgr, dfrgui, regedit and resmon"),
        ],
        test="Your table lists at least twelve tools with the correct launch command for each; the Task Manager triage names the top CPU, memory and disk consumer; and the Event Viewer record cites at least one real error with its source and event ID.",
    ),
    dict(
        num=33, topic=6,
        title="File Systems, Permissions and Share Configuration",
        objective="Compare NTFS, FAT32 and exFAT, and configure NTFS and share permissions correctly, applying the most-restrictive rule (Core 2 objectives 1.9 and 2.5).",
        desc="You compare the Windows file systems on the limits that decide which to use, then configure both NTFS and share permissions on the same folder and prove the most-restrictive rule — the single most misunderstood permission behaviour in Windows.",
        build="A file system comparison table and a configured share demonstrating the effective permission when NTFS and share permissions differ.",
        services="Windows PC, File Explorer, icacls, net share, Disk Management",
        steps=[
            ("Build the comparison table for NTFS, FAT32 and exFAT with columns for maximum file size, maximum volume size, permissions support, encryption, journaling and compatibility.", ""),
            ("Record the decisive limits: FAT32 cannot hold a file larger than 4 GB, exFAT removes that limit but has no permissions, and NTFS supports permissions, encryption, compression and journaling.", ""),
            ("Record which file system to choose for a USB drive shared with macOS, an internal Windows system drive, and a drive holding files larger than 4 GB.", ""),
            ("Confirm the file system on your own volumes.", "wmic logicaldisk get name,filesystem,size,freespace"),
            ("Create a test folder structure for the permissions exercise.", "md C:\\aplus-share\\data && echo Sensitive content > C:\\aplus-share\\data\\test.txt"),
            ("Display the current NTFS permissions and identify which are inherited from the parent folder.", "icacls C:\\aplus-share"),
            ("Grant a specific user read-only NTFS permission on the folder and confirm the change.", "icacls C:\\aplus-share /grant Users:(OI)(CI)R && icacls C:\\aplus-share"),
            ("Record the standard NTFS permission levels in increasing order: Read, Read and Execute, List Folder Contents, Write, Modify and Full Control.", ""),
            ("Explain inheritance: child files and folders inherit permissions from the parent unless inheritance is explicitly broken, and record why breaking inheritance must be deliberate.", ""),
            ("Share the folder over the network and set a share permission that differs from the NTFS permission.", "net share aplusshare=C:\\aplus-share /grant:Everyone,FULL && net share aplusshare"),
            ("State the effective permission for a user with Full Control at the share level and Read at the NTFS level, and confirm the most-restrictive rule gives Read.", ""),
            ("Record that NTFS permissions apply both locally and over the network while share permissions apply only over the network, then remove the share and clean up.", "net share aplusshare /delete && rd /s /q C:\\aplus-share"),
        ],
        flow=[
            ("Compare file systems", "NTFS, FAT32 and exFAT on size, permissions and reach"),
            ("Recall the hard limit", "FAT32 cannot hold a file larger than 4 GB"),
            ("Read the ACL", "icacls shows granted rights and what is inherited"),
            ("Grant and share", "Set an NTFS right, then share with a different one"),
            ("Apply most-restrictive", "Share Full Control with NTFS Read yields Read"),
        ],
        test="Your comparison table states the 4 GB FAT32 file size limit and identifies which file systems support permissions; icacls confirms the granted permission; and you correctly state that Share Full Control combined with NTFS Read yields an effective permission of Read.",
    ),
    dict(
        num=34, topic=6,
        title="Linux Command Line Essentials on Killercoda",
        objective="Use the Linux commands named in Core 2 objective 1.11 for navigation, file management, searching, process control and package management (Core 2 objective 1.11).",
        desc="You work every Linux command the A+ objectives name, on a real Ubuntu machine in the browser. Each command is run against real files and real output so the behaviour, not just the syntax, is what you take away.",
        build="A verified Linux command reference with real output recorded for each command in the objective list.",
        services="Killercoda Ubuntu Playground (https://killercoda.com/playgrounds/scenario/ubuntu)",
        steps=[
            ("Open https://killercoda.com/playgrounds/scenario/ubuntu and wait for the terminal. Confirm where you are and who you are running as.", "pwd && whoami && id"),
            ("Install the packages this lab uses and note that apt is the package manager on Debian and Ubuntu.", "apt-get update -qq && apt-get install -y tree htop nano less"),
            ("Create a working directory tree and confirm its structure visually.", "mkdir -p ~/aplus/{docs,logs,scripts} && tree ~/aplus"),
            ("Create files with content so the later commands have real data to operate on.", "echo 'ERROR: disk full on /dev/sda1' > ~/aplus/logs/system.log && echo 'INFO: service started' >> ~/aplus/logs/system.log && echo 'ERROR: network timeout' >> ~/aplus/logs/system.log"),
            ("List files with the long format showing permissions, ownership, size and modification time, including hidden files.", "ls -la ~/aplus/logs"),
            ("Display file contents with cat, then page through a longer file with less to see how each is used.", "cat ~/aplus/logs/system.log && ls -la /etc | less -E -X | head -20"),
            ("Search file contents with grep, which is the command you use to find an error in a log.", "grep -n 'ERROR' ~/aplus/logs/system.log && grep -c 'ERROR' ~/aplus/logs/system.log"),
            ("Find files by name and by type across a directory tree.", "find /etc -name '*.conf' -type f 2>/dev/null | head -10"),
            ("Copy, move and rename files, then confirm the result of each operation.", "cp ~/aplus/logs/system.log ~/aplus/docs/backup.log && mv ~/aplus/docs/backup.log ~/aplus/docs/archived.log && ls -l ~/aplus/docs"),
            ("Check storage and memory usage, which are the first two things to check on a Linux machine reported as slow.", "df -h && du -sh ~/aplus && free -h"),
            ("List running processes and identify the top consumers, then record how you would terminate a runaway process.", "ps aux --sort=-%mem | head -8 && echo 'Terminate with: kill <PID> or kill -9 <PID>'"),
            ("Change file permissions and ownership, then verify both, and record the numeric meaning of the mode you set.", "chmod 640 ~/aplus/logs/system.log && ls -l ~/aplus/logs/system.log && echo '640 = owner rw-, group r--, others ---'"),
        ],
        flow=[
            ("Open the playground", "Confirm the working directory and the current user"),
            ("Build a tree", "Directories and files with real content to work on"),
            ("List and read", "ls -la, cat and less on real system files"),
            ("Search the content", "grep finds the errors; find locates the files"),
            ("Check and control", "df, free, ps, then chmod and chown"),
        ],
        test="Every command runs without error on the playground; grep returns exactly two ERROR lines from the log; ls -l confirms the 640 permission as rw-r-----; and your reference records real output for each command rather than expected output.",
    ),
    dict(
        num=35, topic=6,
        title="Linux Permissions, Ownership and User Management",
        objective="Apply the Linux permission model to users, groups and others using symbolic and numeric notation, and manage users and sudo rights (Core 2 objective 1.11).",
        desc="You work the permission model until numeric notation is automatic, then apply it to a real access-control problem: giving a group shared write access to a directory without giving everyone else access. This is exactly what the exam tests and what the job requires.",
        build="A permission notation conversion table and a working shared group directory with verified access control.",
        services="Killercoda Ubuntu Playground",
        steps=[
            ("Open the Killercoda playground and examine a file's permission string, identifying the three permission triplets for owner, group and others.", "touch ~/permtest.txt && ls -l ~/permtest.txt"),
            ("Record the notation: r equals 4, w equals 2, x equals 1, so rwx is 7, rw- is 6, r-x is 5 and r-- is 4.", ""),
            ("Build the conversion table for the modes you will actually see: 777, 755, 750, 700, 644, 640 and 600, giving the symbolic form and the typical use of each.", ""),
            ("Set permissions numerically and verify the symbolic result matches your table.", "chmod 644 ~/permtest.txt && ls -l ~/permtest.txt && chmod 600 ~/permtest.txt && ls -l ~/permtest.txt"),
            ("Set permissions symbolically and confirm it produces the same result as the numeric form.", "chmod u=rw,g=r,o= ~/permtest.txt && ls -l ~/permtest.txt"),
            ("Record why directories need the execute bit: without x on a directory you cannot enter it or access anything inside, even with read permission.", "mkdir ~/testdir && chmod 644 ~/testdir && (cd ~/testdir 2>&1 || echo 'Cannot enter: no execute bit') && chmod 755 ~/testdir && cd ~/testdir && pwd"),
            ("Create two users to demonstrate real access control between accounts.", "useradd -m alice && useradd -m bob && echo 'alice:Pass2026!' | chpasswd && echo 'bob:Pass2026!' | chpasswd"),
            ("Create a shared group and add both users to it.", "groupadd support && usermod -aG support alice && usermod -aG support bob && groups alice && groups bob"),
            ("Create a shared directory owned by the group with permissions that allow the group to write but exclude everyone else.", "mkdir -p /srv/shared && chown root:support /srv/shared && chmod 770 /srv/shared && ls -ld /srv/shared"),
            ("Set the setgid bit so that files created inside inherit the group, which is what makes shared directories actually work in practice.", "chmod g+s /srv/shared && ls -ld /srv/shared"),
            ("Verify the access control works: alice can create a file, and the file inherits the support group from the setgid bit.", "su - alice -c 'touch /srv/shared/alice-file.txt' && ls -l /srv/shared"),
            ("Grant alice administrative rights through sudo and record why sudo is preferred over logging in as root.", "usermod -aG sudo alice && groups alice && echo 'sudo gives auditable, per-command elevation rather than a permanent root session'"),
        ],
        flow=[
            ("Read the triplets", "Owner, group and others, each with r, w and x"),
            ("Convert the notation", "r is 4, w is 2, x is 1, so rw- is 6 and rwx is 7"),
            ("Prove the execute bit", "A directory at 644 cannot be entered at all"),
            ("Create users and a group", "Two accounts sharing one support group"),
            ("Share with setgid", "Mode 2770 so new files inherit the group"),
        ],
        test="Your conversion table is correct for all seven modes; the execute-bit demonstration shows a directory becoming enterable at 755 but not at 644; /srv/shared is mode 2770 owned by the support group; and alice's file inherits the support group.",
    ),
    dict(
        num=36, topic=6,
        title="macOS Tools and Cross-Platform File System Compatibility",
        objective="Identify macOS features and utilities and resolve cross-platform file system compatibility between Windows, macOS and Linux (Core 2 objectives 1.10 and 1.9).",
        desc="You map the macOS toolset to its Windows equivalent, which is how a technician supporting a mixed fleet actually thinks, then solve the file system compatibility problem that causes most cross-platform support tickets.",
        build="A macOS-to-Windows tool equivalence table and a solved cross-platform file system compatibility matrix.",
        services="macOS device or reference material, Windows PC, file system documentation",
        steps=[
            ("Build the equivalence table mapping each macOS tool to its Windows counterpart, since knowing one gives you the other.", ""),
            ("Map Time Machine to File History and Windows Backup, and record that Time Machine performs versioned incremental backups to an external or network volume.", ""),
            ("Map Disk Utility to Disk Management, and record that Disk Utility also provides First Aid, which repairs permissions and directory structure.", ""),
            ("Map FileVault to BitLocker as the full-disk encryption feature, and Keychain to Credential Manager as the stored credential vault.", ""),
            ("Map Mission Control to Task View, Spotlight to Windows Search, Finder to File Explorer and Terminal to Command Prompt or PowerShell.", ""),
            ("Record Gatekeeper's role: it controls which applications may run based on their signature, and note that installing from outside the App Store requires explicitly allowing it.", ""),
            ("Record the macOS software sources in order of trust: the App Store, a signed vendor installer, and an unsigned application which Gatekeeper blocks by default.", ""),
            ("Record the macOS best practices from the objectives: scheduled Time Machine backups, current OS and application updates, and antivirus where organisational policy requires it.", ""),
            ("Build the file system compatibility matrix with rows for NTFS, FAT32, exFAT, HFS+, APFS and ext4 and columns for Windows, macOS and Linux read and write support.", ""),
            ("Record the critical compatibility fact: macOS reads NTFS but cannot write to it without third-party software, which is the cause of most cross-platform ticket escalations.", ""),
            ("Solve the scenario of an external drive that must be read and written by Windows and macOS with files larger than 4 GB, and justify exFAT as the only correct answer.", ""),
            ("Solve the scenario of a drive that must work with Windows, macOS and a digital camera, and justify FAT32 while stating its 4 GB limitation explicitly.", ""),
        ],
        flow=[
            ("Map the equivalents", "Time Machine, Disk Utility, FileVault and Keychain"),
            ("Explain Gatekeeper", "It decides which applications are allowed to run"),
            ("Rank the sources", "App Store, signed installer, then unsigned"),
            ("Table the file systems", "Read and write support across the three platforms"),
            ("Solve the exchange", "macOS reads NTFS but cannot write to it"),
        ],
        test="Your equivalence table maps at least eight macOS tools to their Windows counterparts; the compatibility matrix correctly shows macOS as read-only for NTFS; and both scenarios name a file system and justify it against the stated constraints.",
    ),
]
