"""Domain 2 — Networking (Core 1, 23%). Labs 5–11."""

DOMAIN2 = [
    dict(
        num=5, topic=2,
        title="Protocols and Ports — Building the A+ Port Reference",
        objective="Identify the TCP/UDP ports, transport protocol and secure replacement for every protocol on the CompTIA A+ Core 1 objective 2.1 list.",
        desc="You build the complete A+ port reference table from live evidence rather than memorisation: you query each service on a Killercoda Ubuntu playground, observe which transport it uses, and record the secure alternative where the protocol is insecure. The table becomes your revision sheet.",
        build="A complete 16-row port reference table with port number, transport, purpose, security status and secure replacement.",
        services="Killercoda Ubuntu Playground (https://killercoda.com/playgrounds/scenario/ubuntu), ss, nmap, /etc/services",
        steps=[
            ("Open https://killercoda.com/playgrounds/scenario/ubuntu in your browser and wait for the terminal prompt to appear. You have root access and nothing is installed on your own machine.", ""),
            ("Update the package list and install the networking tools you need for this lab.", "apt-get update -qq && apt-get install -y net-tools iproute2 nmap dnsutils curl netcat-openbsd"),
            ("Look up each A+ protocol in the system services database to confirm its official port and transport.", "grep -wE 'ftp|ssh|telnet|smtp|domain|bootps|bootpc|http|pop3|netbios-ssn|imap2|snmp|ldap|https|microsoft-ds' /etc/services"),
            ("Record the port numbers for the file transfer and remote access group: FTP 20/21, SSH and SFTP 22, Telnet 23, RDP 3389.", "grep -wE 'ftp-data|ftp|ssh|telnet' /etc/services | head -20"),
            ("Record the mail group: SMTP 25, POP3 110, IMAP 143, and note the secure TLS variants SMTPS 587/465, POP3S 995 and IMAPS 993.", "grep -wE 'smtp|pop3|imap2|imaps|pop3s|submission' /etc/services"),
            ("Record the infrastructure group: DNS 53, DHCP 67 server and 68 client, and note that DNS uses UDP for queries and TCP for zone transfers.", "grep -wE 'domain|bootps|bootpc' /etc/services"),
            ("Prove DNS runs over UDP by querying a public resolver and observing the transport used.", "dig +noall +stats google.com @8.8.8.8"),
            ("Confirm HTTP on 80 and HTTPS on 443 by requesting headers from a live site and reading the response code.", "curl -sI https://www.tertiarycourses.com.sg | head -5"),
            ("List every listening socket on the playground with its protocol, port and owning process, and identify which are TCP and which are UDP.", "ss -tulnp"),
            ("Start a listener on a chosen port, then scan it from the same host to see how a scanner reports an open port.", "nc -l -p 8080 & sleep 1 && nmap -sT -p 8080 127.0.0.1"),
            ("Complete the reference table with a Security column marking FTP, Telnet, HTTP, SNMPv1/v2 and POP3 as insecure, each with its secure replacement named.", ""),
            ("Add the final rows for NetBIOS 137–139, SNMP 161/162, LDAP 389 and SMB/CIFS 445, then verify your table has all 16 protocols.", ""),
        ],
        flow=[
            ("Open the playground", "A real Ubuntu terminal, root access, in the browser"),
            ("Install the tools", "Refresh the package index, then add the net tools"),
            ("Read the port list", "Confirm each protocol in the services database"),
            ("Prove the transport", "dig shows DNS over UDP; curl confirms HTTP and HTTPS"),
            ("List live sockets", "ss -tulnp shows what is listening and on which port"),
        ],
        test="Your table lists all 16 protocols with the correct port, transport and purpose; every insecure protocol has its secure replacement named; and ss -tulnp output confirms at least one live listening socket with its transport.",
    ),
    dict(
        num=6, topic=2,
        title="IPv4 Subnetting and Address Planning with IP Calculator",
        objective="Calculate subnet masks, network and broadcast addresses, usable host ranges and CIDR notation, and design an address plan for a SOHO network (Core 1 objective 2.5).",
        desc="You use IP Calculator to work subnetting from both directions — decoding a given address and designing a plan to meet a host requirement — then verify every result by hand so you can reproduce it in an exam where no calculator is allowed.",
        build="A verified subnetting worksheet and a complete four-subnet SOHO address plan exported as CSV.",
        services="IP Calculator (https://alfredang.github.io/ipcalculator/), Killercoda Ubuntu Playground, ipcalc",
        steps=[
            ("Open IP Calculator at https://alfredang.github.io/ipcalculator/ and select the IPv4 tab.", ""),
            ("Enter 192.168.10.75/24 and record the network address, broadcast address, first and last usable host, netmask in dotted decimal, and the usable host count.", ""),
            ("Change the prefix to /26 with the same address and record how the network address, broadcast address and usable host count change.", ""),
            ("Verify the /26 result by hand: 32 minus 26 leaves 6 host bits, 2 to the power 6 is 64 addresses per block, minus 2 reserved gives 62 usable hosts. Confirm this matches the tool.", ""),
            ("Enter 172.16.0.0/12 and 10.0.0.0/8 in turn, and confirm from the output that all three RFC 1918 private ranges are non-routable on the internet.", ""),
            ("Enter 169.254.14.9/16 and record what this range signifies — an APIPA address assigned when no DHCP server answered.", ""),
            ("Design a SOHO plan from the 192.168.20.0/24 block for four departments needing 50, 25, 10 and 5 hosts. Choose the smallest prefix that satisfies each requirement.", ""),
            ("Verify each chosen prefix in IP Calculator: /26 gives 62 usable for the 50-host subnet, /27 gives 30 for the 25-host subnet, /28 gives 14 for the 10-host subnet and /29 gives 6 for the 5-host subnet.", ""),
            ("Switch to the Batch tab, paste all four subnets one per line with a comment naming each department, and process them together.", ""),
            ("Export the batch results to CSV using the export control, and keep the file as the address-plan deliverable.", ""),
            ("Open the Killercoda Ubuntu playground and install ipcalc to cross-check your work with an independent tool.", "apt-get update -qq && apt-get install -y ipcalc"),
            ("Cross-check the first subnet on the command line and confirm the network, broadcast and host range match IP Calculator exactly.", "ipcalc 192.168.20.0/26"),
        ],
        flow=[
            ("Decode an address", "Enter 192.168.10.75/24 and read the full breakdown"),
            ("Change the prefix", "Watch network, broadcast and host count change"),
            ("Check it by hand", "32 minus 26 gives 6 host bits, 64 block, 62 usable"),
            ("Plan four subnets", "Smallest prefix that meets 50, 25, 10 and 5 hosts"),
            ("Export and confirm", "Batch to CSV, then cross-check with ipcalc"),
        ],
        test="Every subnet in your plan satisfies its host requirement with no wasted block larger than necessary, the hand calculation matches the IP Calculator output for the /26 case, ipcalc agrees with the browser tool, and the CSV export contains all four subnets.",
    ),
    dict(
        num=7, topic=2,
        title="Network Devices and Traffic Behaviour Analysis",
        objective="Distinguish hub, switch, router, firewall and access point by the layer they operate at and the traffic behaviour each produces (Core 1 objectives 2.2 and 2.3).",
        desc="Rather than reading device descriptions, you infer device behaviour from captured traffic. Using PCAP Analyzer you examine a capture, identify the MAC and IP conversations, and reason about which device forwarded each frame and why.",
        build="A device comparison matrix and a written traffic analysis identifying the forwarding decision at each layer.",
        services="PCAP Analyzer (https://alfredang.github.io/pcapanalyzer/), device reference diagrams",
        steps=[
            ("Open PCAP Analyzer at https://alfredang.github.io/pcapanalyzer/ and click the Sample button to generate a demonstration capture. Nothing is uploaded — parsing happens in your browser.", ""),
            ("Record the four dashboard metrics: packet count, total bytes, capture duration and average packet size.", ""),
            ("Open the protocol distribution view and list every protocol present with its share of the capture.", ""),
            ("Open Top talkers and record the three most active endpoints by traffic volume, noting their addresses.", ""),
            ("Open Top conversations and identify which pairs of hosts exchange the most data, then state what kind of session each pair most likely represents.", ""),
            ("Select an individual packet in the packets table and open its detail view to read the source, destination, protocol and length.", ""),
            ("From the packet detail, identify the Layer 2 MAC addresses and the Layer 3 IP addresses, and explain which one a switch uses to forward and which one a router uses.", ""),
            ("Build the device comparison matrix with rows for hub, switch, router, firewall and access point and columns for OSI layer, forwarding basis, collision domains, broadcast domains and typical use.", ""),
            ("Explain in writing why a hub creates one collision domain across all ports while a switch creates one per port, and what that means for network performance.", ""),
            ("Explain why a switch forwards a broadcast frame out of every port but a router does not forward it at all, and connect this to why VLANs are needed.", ""),
            ("Apply the protocol filter in the packets table to isolate one protocol, and record how the packet count changes.", ""),
            ("Write a short conclusion stating what the capture reveals about the network — how many broadcast domains are visible, and whether a router is present in the path.", ""),
        ],
        flow=[
            ("Load a capture", "Click Sample; parsing happens in your own browser"),
            ("Read the headline", "Packets, bytes, duration and average packet size"),
            ("Split by protocol", "Record each protocol and its share of the capture"),
            ("Follow the talkers", "Top conversations show which pair to investigate"),
            ("Place each device", "Hub, switch, router, firewall and AP by OSI layer"),
        ],
        test="Your matrix correctly assigns each of the five devices to its OSI layer and forwarding basis; your analysis names the specific MAC and IP addresses observed; and the conclusion is supported by evidence from the capture rather than from theory alone.",
    ),
    dict(
        num=8, topic=2,
        title="Wireless Standards, Channel Planning and Interference",
        objective="Compare 802.11 standards and frequency bands, and design a channel plan that avoids co-channel and adjacent-channel interference (Core 1 objective 2.3).",
        desc="You compare the 2.4 GHz and 5 GHz bands on the criteria that decide real deployments, then design a channel plan for a three-access-point office and justify every channel choice against the non-overlapping channel constraint.",
        build="An 802.11 standards comparison table and a justified three-AP channel plan with a coverage sketch.",
        services="Wi-Fi analyser application on a mobile device or laptop, 802.11 standards reference, floor plan sketch",
        steps=[
            ("Build the standards table with rows for 802.11a, b, g, n, ac and ax and columns for frequency band, maximum theoretical throughput, channel width and backward compatibility.", ""),
            ("Record that 2.4 GHz offers only three non-overlapping channels — 1, 6 and 11 — and explain from channel width why any other choice overlaps.", ""),
            ("Record that 5 GHz offers around 24 non-overlapping 20 MHz channels, and note the trade-off: more channels and more speed, but shorter range and poorer wall penetration.", ""),
            ("Open a Wi-Fi analyser on your device and record every visible SSID with its channel, band and signal strength in dBm.", ""),
            ("Identify from your scan which 2.4 GHz channels are most congested in your location and which of 1, 6 or 11 is least used.", ""),
            ("Interpret the signal strengths you recorded: better than -50 dBm is excellent, -60 is good, -70 is usable and worse than -80 is unreliable.", ""),
            ("Sketch a three-room office floor plan and place three access points to give overlapping coverage with no dead zones.", ""),
            ("Assign 2.4 GHz channels 1, 6 and 11 to the three access points so that no two adjacent cells share a channel, and mark each on the sketch.", ""),
            ("Explain the difference between co-channel interference, where cells share a channel and must take turns, and adjacent-channel interference, where overlapping channels corrupt each other's transmissions.", ""),
            ("List five physical sources of 2.4 GHz interference in a typical office — microwave ovens, cordless phones, Bluetooth devices, fluorescent ballasts and thick or metal-reinforced walls.", ""),
            ("State the security configuration for all three access points: WPA3 where supported, WPA2 with AES-CCMP as the minimum, and never WEP or WPA with TKIP.", ""),
            ("Write the justification paragraph explaining why your channel assignment minimises interference, referencing the non-overlapping constraint explicitly.", ""),
        ],
        flow=[
            ("Table the standards", "802.11a through ax on band, speed and width"),
            ("Scan the air", "Record every SSID with its channel and dBm"),
            ("Read the strength", "-50 excellent, -60 good, -70 usable, -80 unreliable"),
            ("Plan three cells", "Assign 1, 6 and 11 so no neighbours share a channel"),
            ("Justify the plan", "Separate co-channel from adjacent-channel interference"),
        ],
        test="Your channel plan uses only channels 1, 6 and 11 in the 2.4 GHz band with no adjacent cells sharing a channel; the scan records real SSIDs with channel and dBm; and the justification correctly distinguishes co-channel from adjacent-channel interference.",
    ),
    dict(
        num=9, topic=2,
        title="SOHO Network Configuration — DHCP, DNS, NAT and Port Forwarding",
        objective="Configure a SOHO router including DHCP scope, DNS, NAT, DHCP reservations and port forwarding, and verify each service from a client (Core 1 objective 2.5).",
        desc="You configure the full service set a SOHO router provides, then verify each one from a client using the command line. Where physical hardware is unavailable you use the Killercoda playground to inspect and reason about the same configuration from the client side.",
        build="A documented SOHO configuration with DHCP scope, reservation, DNS and port-forwarding rule, each verified from a client.",
        services="SOHO router or router emulator, Killercoda Ubuntu Playground, IP Calculator, ipconfig/ip, nslookup/dig",
        steps=[
            ("Plan the addressing before touching the router: choose 192.168.50.0/24, reserve .1 for the gateway, .2 to .99 for static assignments and .100 to .250 for the DHCP scope.", ""),
            ("Verify the plan in IP Calculator by entering 192.168.50.0/24 and confirming the usable host range covers every allocation you made.", ""),
            ("Configure the DHCP scope on the router with the start and end addresses from your plan, and set the lease time.", ""),
            ("Define the exclusion range covering .2 to .99 so the DHCP server never hands out an address reserved for static assignment.", ""),
            ("Create a DHCP reservation binding a printer's MAC address to a fixed address such as 192.168.50.20, so it always receives the same IP.", ""),
            ("Set the DNS servers the router hands to clients, and record whether you used the ISP resolver or a public one such as 8.8.8.8 or 1.1.1.1.", ""),
            ("On a client, release and renew the DHCP lease and record the address, mask, gateway and DNS servers received.", "ipconfig /release && ipconfig /renew && ipconfig /all"),
            ("On the Killercoda playground, inspect the equivalent client-side configuration and identify the interface address, mask and default route.", "ip addr show && ip route show && cat /etc/resolv.conf"),
            ("Verify DNS resolution works through the configured resolver and record the answer section of the response.", "dig +noall +answer www.tertiarycourses.com.sg"),
            ("Explain how NAT lets many private hosts share one public address, and identify from the router status page what your public address is.", ""),
            ("Create a port-forwarding rule directing external TCP 3389 to an internal host, and state the security risk of exposing RDP directly to the internet.", ""),
            ("Document the complete configuration in a table a colleague could use to rebuild the router from scratch after a factory reset.", ""),
        ],
        flow=[
            ("Plan the addressing", "Gateway, static block and DHCP scope before you start"),
            ("Set the DHCP scope", "Range, lease and an exclusion for the static block"),
            ("Reserve by MAC", "The printer always receives the same address"),
            ("Verify from a client", "Release, renew, then read address, gateway and DNS"),
            ("Forward a port", "Document the rule and state the security risk"),
        ],
        test="The client receives an address inside the DHCP scope and outside the exclusion range, the reservation returns the same address after a release and renew, DNS resolves successfully through the configured resolver, and the port-forwarding rule is documented with its security risk stated.",
    ),
    dict(
        num=10, topic=2,
        title="Network Cabling — Termination, Testing and Tool Selection",
        objective="Terminate twisted-pair cable to T568B, test it end to end, and select the correct tool for each cabling task (Core 1 objectives 2.3 and 3.1).",
        desc="You terminate a patch cable to the T568B standard, test it with a cable tester, and deliberately create a fault to see how the tester reports it. You then build a tool selection guide matching each networking tool to the single question it answers.",
        build="A terminated and tested patch cable, a fault-injection test record, and a tool selection guide.",
        services="Crimper, RJ45 plugs, Cat 5e/6 cable, cable stripper, cable tester, toner probe, punch-down tool, loopback plug",
        steps=[
            ("Write out the T568B colour order from memory before you begin: white-orange, orange, white-green, blue, white-blue, green, white-brown, brown.", ""),
            ("Strip about 25 mm of the outer jacket with the cable stripper, taking care not to nick the insulation of the individual conductors.", ""),
            ("Untwist and straighten the four pairs, then arrange all eight conductors flat in the T568B order you wrote down.", ""),
            ("Trim the conductors square to about 12 mm so they will all reach the end of the plug while the jacket still enters the strain relief.", ""),
            ("Insert the conductors fully into the RJ45 plug, confirm through the clear plastic that each wire reaches the end and the order is still correct, then crimp firmly.", ""),
            ("Terminate the far end to T568B as well, which makes this a straight-through patch cable rather than a crossover.", ""),
            ("Test the cable with the cable tester and confirm the lights advance 1 through 8 in sequence on both the main unit and the remote.", ""),
            ("Deliberately make a second cable with two conductors swapped, test it, and record how the tester reports the crossed pair.", ""),
            ("Make a third cable with one conductor not fully seated, test it, and record how the tester reports the open circuit.", ""),
            ("Use the toner probe on a bundle: attach the tone generator to one known end and use the probe to find the matching far end.", ""),
            ("Terminate a cable to a patch panel or keystone jack with the punch-down tool, noting that the blade trims the excess conductor as it seats.", ""),
            ("Build the tool selection guide: crimper terminates plugs, stripper removes the jacket, cable tester verifies the pinout, toner probe identifies a cable in a bundle, punch-down seats wires in a block, loopback plug tests a port, and a Wi-Fi analyser shows channel congestion.", ""),
        ],
        flow=[
            ("Recall T568B", "Write the eight-wire colour order before you cut"),
            ("Strip and order", "25 mm of jacket, conductors flat in T568B order"),
            ("Crimp both ends", "Same standard both ends makes a straight-through cable"),
            ("Test the pinout", "All eight lights advance in sequence on both units"),
            ("Inject a fault", "Swap a pair, then leave one open, and compare results"),
        ],
        test="Your good cable passes the tester with all eight lights in sequence on both ends; the two faulty cables produce distinctly different tester results that you have recorded; and the tool guide matches all seven tools to the specific question each answers.",
    ),
    dict(
        num=11, topic=2,
        title="Packet Capture Analysis — Reading Real Network Traffic",
        objective="Analyse a packet capture to identify protocols, conversations and anomalies, and use the evidence to support a network diagnosis (Core 1 objectives 2.1 and 5.7).",
        desc="You work a capture the way a support engineer does: start from the summary statistics, narrow by protocol, follow the largest conversation, and read individual packets only once you know which ones matter. The output is a written diagnosis supported by named evidence.",
        build="A written traffic analysis report with protocol breakdown, conversation analysis and a supported diagnosis.",
        services="PCAP Analyzer (https://alfredang.github.io/pcapanalyzer/), Killercoda Ubuntu Playground, tcpdump",
        steps=[
            ("Open the Killercoda Ubuntu playground and install tcpdump so you can generate your own capture rather than only using sample data.", "apt-get update -qq && apt-get install -y tcpdump curl"),
            ("Start a capture on the default interface, writing to a file, and limit it to 200 packets so the capture ends by itself.", "tcpdump -i any -c 200 -w /root/capture.pcap &"),
            ("While the capture runs, generate varied traffic so the capture contains several protocols to analyse.", "sleep 2; curl -s https://example.com > /dev/null; curl -s http://neverssl.com > /dev/null; getent hosts google.com"),
            ("Wait for the capture to finish and confirm the file exists with a non-zero size.", "wait; ls -lh /root/capture.pcap"),
            ("Read the capture summary on the command line to see what was captured before moving to the browser tool.", "tcpdump -r /root/capture.pcap -nn -c 20"),
            ("Download the capture file from the playground, or if download is unavailable, open PCAP Analyzer and use the Sample button instead.", ""),
            ("Open https://alfredang.github.io/pcapanalyzer/ and load the capture by dragging the file onto the drop area or using the file browser.", ""),
            ("Record the four dashboard statistics and the detected file format, then state what the average packet size suggests about the traffic mix.", ""),
            ("Examine the protocol distribution and record each protocol with its share, then identify which protocols are encrypted and which are in clear text.", ""),
            ("Open Top conversations, select the pair exchanging the most data, and state what kind of session it represents based on the ports involved.", ""),
            ("Filter the packets table to a single protocol, select a packet, and read its detail and hex view to identify the source port, destination port and payload length.", ""),
            ("Write the diagnosis: state one observation about the network that the capture supports, name the specific packets or conversations that evidence it, and state what you would capture next to confirm it.", ""),
        ],
        flow=[
            ("Capture live traffic", "tcpdump writes 200 packets to a file on the playground"),
            ("Generate the traffic", "curl and a DNS lookup give several protocols to read"),
            ("Load the analyser", "Drag the file in, or use Sample if download is blocked"),
            ("Read the mix", "Protocol distribution shows what is encrypted and what is not"),
            ("Write the diagnosis", "Cite specific addresses, ports and packet numbers"),
        ],
        test="tcpdump produces a capture file containing at least three distinct protocols, PCAP Analyzer loads it and reports the protocol distribution, and your written diagnosis cites specific addresses, ports and packet numbers as evidence rather than general statements.",
    ),
]
