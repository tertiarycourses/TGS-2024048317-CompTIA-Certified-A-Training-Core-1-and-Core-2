"""Domain 5 — Hardware and Network Troubleshooting (Core 1, 28%). Labs 22–28."""

DOMAIN5 = [
    dict(
        num=22, topic=5,
        title="Applying the CompTIA Six-Step Troubleshooting Methodology",
        objective="Apply the CompTIA six-step troubleshooting methodology in order to a real fault, documenting each step (Core 1 objective 5.1).",
        desc="The methodology is examinable in order and is the framework every other troubleshooting lab in this course hangs from. You apply all six steps to a fault, writing the evidence at each step, and produce the documentation that step six actually requires.",
        build="A completed six-step troubleshooting record for one fault, with evidence and a preventive measure at each step.",
        services="Training PC with an injected fault, ticketing template, Killercoda Ubuntu Playground",
        steps=[
            ("Write the six steps in order before you start, because the order itself is examinable: identify the problem; establish a theory of probable cause; test the theory; establish a plan of action and implement it; verify full functionality and implement preventive measures; document findings, actions and outcomes.", ""),
            ("Step 1 — identify the problem. Interview the user with open questions: what exactly happens, when did it start, what changed, and can you reproduce it on demand.", ""),
            ("Step 1 continued — always ask what changed, because most faults follow a recent change: a new driver, an update, a moved cable or new software.", ""),
            ("Step 1 continued — back up user data before making any change, so that your troubleshooting can never be the cause of data loss.", ""),
            ("Step 2 — establish a theory of probable cause, questioning the obvious first and listing at least three candidate causes ranked by likelihood.", ""),
            ("Step 3 — test the theory. Design a test that will disprove the theory if it is wrong, not merely one that confirms what you already believe.", ""),
            ("Step 3 continued — if the theory is not confirmed, establish a new theory or escalate. Record that escalation is a legitimate step in the methodology, not a failure.", ""),
            ("Step 4 — establish a plan of action, referring to vendor documentation, and state the rollback position before you implement anything.", ""),
            ("Step 5 — verify full system functionality, testing not only the reported fault but the functions around it that your change might have affected.", ""),
            ("Step 5 continued — implement preventive measures so the same fault does not recur, and state the specific measure you applied.", ""),
            ("Step 6 — document findings, actions and outcomes in the ticket, in language a colleague could follow without speaking to you.", ""),
            ("Practise the discipline on the Killercoda playground by diagnosing a deliberately broken DNS configuration through all six steps and recording your evidence.", "cp /etc/resolv.conf /root/resolv.backup && echo 'nameserver 192.0.2.1' > /etc/resolv.conf && (dig +short +time=2 +tries=1 google.com || echo 'STEP 1: resolution FAILS - symptom confirmed') && cp /root/resolv.backup /etc/resolv.conf && dig +short google.com"),
        ],
        flow=[
            ("Learn the six steps", "Identify, theorise, test, plan, verify, then document"),
            ("Interview and back up", "Ask what changed, and protect the data first"),
            ("Design a real test", "One that would disprove the theory if it were wrong"),
            ("Plan with a rollback", "State the way back before you change anything"),
            ("Verify and document", "Prove the fix, prevent recurrence, write it up"),
        ],
        test="Your record contains all six steps in the correct order, each with written evidence; a rollback position is stated before implementation; a specific preventive measure is named; and the DNS exercise shows the fault reproduced and then resolved.",
    ),
    dict(
        num=23, topic=5,
        title="POST, Boot and Power Fault Diagnosis",
        objective="Diagnose no-power, no-POST and no-boot faults using beep codes, diagnostic LEDs and a systematic elimination sequence (Core 1 objective 5.2).",
        desc="A machine that will not start gives you almost no information, so you need a fixed elimination sequence rather than guesswork. You build that sequence, learn to read the signals the board does give — beep codes and LEDs — and separate the three distinct failure classes.",
        build="A three-branch diagnostic flowchart for no-power, no-POST and no-boot, plus a beep code and LED reference.",
        services="Training PC, motherboard manual, POST diagnostic references, PSU tester or multimeter",
        steps=[
            ("Separate the three failure classes precisely, because they have different causes: no power means nothing happens at all, no POST means fans spin but nothing appears on screen, and no boot means POST completes but the OS does not load.", ""),
            ("Build the no-power branch: check the wall socket and the mains lead, check the PSU switch, check the case power switch header, test or substitute the PSU, then suspect the motherboard.", ""),
            ("Build the no-POST branch: listen for beep codes, read diagnostic LEDs or the POST code display, reseat RAM, reseat the graphics card, disconnect all non-essential devices, then clear CMOS.", ""),
            ("Record the beep code principle: patterns are manufacturer-specific and must be read from that board's manual, but a repeating pattern most commonly indicates RAM or graphics.", ""),
            ("Record the reseating principle: reseating RAM and expansion cards costs nothing and resolves a large share of no-POST faults caused by vibration, thermal cycling or transport.", ""),
            ("Build the no-boot branch: check the boot order, remove any bootable USB device, confirm the drive is detected in firmware, then repair the boot record from the recovery environment.", ""),
            ("Record the minimum configuration test: strip the system to CPU, one RAM stick, integrated graphics and the PSU. If it POSTs, add components back one at a time until the fault returns.", ""),
            ("Map the remaining hardware symptoms to causes: a burning smell means shut down and unplug immediately, a grinding noise means a failing fan or drive, and capacitor swelling means the board must be replaced.", ""),
            ("Record that an inaccurate system date and time that resets on every boot means the CMOS battery is dead, and that this is a two-dollar part rather than a board fault.", ""),
            ("Record the intermittent shutdown checklist: overheating, a failing PSU, a loose power connector, or a short from a misplaced standoff behind the board.", ""),
            ("Record the sluggish performance checklist: insufficient RAM, a failing or nearly full disk, thermal throttling, malware, or too many startup applications.", ""),
            ("Assemble the three branches into one flowchart with a clear entry question that routes a technician to the correct branch within two questions.", ""),
        ],
        flow=[
            ("Classify the failure", "No power, no POST, or POST completes but no boot"),
            ("Work the power branch", "Socket, lead, switch, PSU, then the board itself"),
            ("Read what it tells you", "Beep codes and diagnostic LEDs before any teardown"),
            ("Reseat and strip back", "RAM and graphics, then the minimum configuration"),
            ("Fix the boot path", "Boot order, stray USB, disk detection, boot record"),
        ],
        test="Your flowchart routes to the correct branch from the entry symptom in no more than two questions, each branch orders its checks from cheapest and least invasive to most, and the minimum configuration test is included in the no-POST branch.",
    ),
    dict(
        num=24, topic=5,
        title="Storage and RAID Fault Diagnosis with S.M.A.R.T.",
        objective="Diagnose storage faults using S.M.A.R.T. data, symptom analysis and RAID status, and choose the correct recovery action for each RAID level (Core 1 objective 5.3).",
        desc="Storage faults are the ones where a wrong move destroys the data permanently, so the order of actions matters more than in any other troubleshooting lab. You read S.M.A.R.T. attributes, interpret the warning signs, and decide the recovery action per RAID level.",
        build="A S.M.A.R.T. attribute interpretation record, a storage symptom map and a RAID recovery decision table.",
        services="Training PC, Killercoda Ubuntu Playground, smartctl, chkdsk, Disk Management, wmic",
        steps=[
            ("Record the first rule of storage troubleshooting: if the data matters and the drive is failing, back it up before you do anything else. Diagnostics can be the final straw for a dying drive.", ""),
            ("Check drive health on Windows using the built-in interface and record the status reported for each physical disk.", "wmic diskdrive get model,status,size"),
            ("Open Disk Management and record each volume's file system, capacity, free space and status.", "diskmgmt.msc"),
            ("Open the Killercoda Ubuntu playground and install the S.M.A.R.T. monitoring tools to read raw drive attributes.", "apt-get update -qq && apt-get install -y smartmontools"),
            ("List the block devices available so you know which device names to query.", "lsblk -o NAME,SIZE,TYPE,MOUNTPOINT"),
            ("Read the S.M.A.R.T. attributes for a device and record the overall health self-assessment result.", "smartctl -H /dev/sda || echo 'Virtual device - review the attribute reference instead'"),
            ("Record the S.M.A.R.T. attributes that matter most: reallocated sector count, current pending sector count, offline uncorrectable and read error rate — any non-zero value on the first three is a replace signal.", ""),
            ("Record the correct response to a S.M.A.R.T. warning: back up immediately and replace the drive. S.M.A.R.T. reports a drive that is already failing, not one that might fail eventually.", ""),
            ("Map the audible and behavioural symptoms: clicking or grinding means imminent mechanical failure, and extended read and write times or falling IOPS mean the drive is retrying failing sectors.", ""),
            ("Map 'bootable device not found' to its three causes: wrong boot order in firmware, a dead drive, or a corrupt boot record — and give the test that distinguishes them.", ""),
            ("Build the RAID recovery table: RAID 1, 5 and 10 survive one disk failure and rebuild after replacement, while RAID 0 has no redundancy so any failure means a full restore from backup.", ""),
            ("Record the RAID rebuild warning: a rebuild puts every remaining disk under sustained full load, which is exactly when a second ageing disk in the same batch tends to fail — so verify the backup before starting one.", ""),
        ],
        flow=[
            ("Protect the data first", "Back up before diagnostics — they can finish a dying drive"),
            ("Check the health", "wmic and Disk Management report status and capacity"),
            ("Read S.M.A.R.T.", "Reallocated, pending and uncorrectable sector counts"),
            ("Act on the warning", "Any non-zero count means back up now and replace"),
            ("Recover the array", "RAID 1, 5 and 10 rebuild; RAID 0 means restore"),
        ],
        test="Your record names at least four S.M.A.R.T. attributes and states the correct action for a non-zero reallocated sector count; the recovery table gives a distinct action for all four RAID levels; and the rebuild risk is stated explicitly.",
    ),
    dict(
        num=25, topic=5,
        title="Display and Projector Fault Diagnosis",
        objective="Diagnose display and projector faults by isolating source, cable, panel, backlight and settings as distinct failure points (Core 1 objective 5.4).",
        desc="Display faults are frequently misdiagnosed as a dead monitor when the real cause is a wrong input, a failing cable or a dead backlight. You build the isolation sequence that identifies which of the five components has actually failed before any part is ordered.",
        build="A display fault isolation sequence and a symptom-to-cause map covering twelve display and projector faults.",
        services="Monitor, projector, spare display cables, training PC, torch, display settings",
        steps=[
            ("Build the isolation sequence with five distinct check points: the source device, the cable, the input selection, the panel and backlight, and the display settings.", ""),
            ("Check the source first: confirm the machine is actually on and producing output, using its own status lights and any secondary display.", ""),
            ("Check the input selection second, because a monitor showing 'no signal' while set to the wrong input is the single most common display complaint and costs nothing to fix.", ""),
            ("Check the cable third by substituting a known-good cable of the same type, and record that a cable can fail intermittently while looking perfectly intact.", ""),
            ("Diagnose 'no image but the backlight is clearly on' as a signal problem — source, cable or input — rather than a panel fault.", ""),
            ("Diagnose 'a very faint image visible only under a torch' as a dead backlight or, on older CCFL panels, a failed inverter. The panel itself is working.", ""),
            ("Diagnose a dim projector image as a bulb nearing end of life, and record that projector bulbs are rated in hours and dim progressively rather than failing suddenly.", ""),
            ("Diagnose a fuzzy or blurry image as either a resolution that is not the panel's native resolution, or a poor analog cable connection on VGA.", ""),
            ("Diagnose display burn-in as a persistent ghost of a static image, note that it affects OLED and plasma most, and record that mitigation is a moving image or a pixel refresh cycle.", ""),
            ("Diagnose dead pixels as permanently black or stuck-colour dots, and record that manufacturers replace a panel only above a threshold count stated in the warranty.", ""),
            ("Diagnose intermittent projector shutdown as either overheating from a blocked filter or intake, or an eco or standby mode triggering on a static image.", ""),
            ("Complete the map with flashing screen, incorrect colour display, audio not passing over HDMI and an image that flickers when the laptop lid is moved, giving the component and confirming test for each.", ""),
        ],
        flow=[
            ("Check the source", "Confirm the machine is on and producing output"),
            ("Check the input", "A wrong input selection is the commonest complaint"),
            ("Substitute the cable", "A cable can fail intermittently while looking intact"),
            ("Isolate the backlight", "A faint image under a torch means backlight or inverter"),
            ("Map the rest", "Burn-in, dead pixels, colour, dim bulb and shutdown"),
        ],
        test="Your isolation sequence checks input selection before substituting any hardware, the map covers all twelve faults with a named component and a confirming test, and the backlight, panel and signal path are treated as three separate failure points.",
    ),
    dict(
        num=26, topic=5,
        title="Mobile Device Hardware Fault Diagnosis",
        objective="Diagnose mobile device hardware faults including battery, charging, screen, digitizer, port and overheating problems, applying the correct safety response (Core 1 objective 5.5).",
        desc="Mobile faults carry a safety dimension that desktop faults do not: a swollen lithium battery is a fire and chemical hazard. You build the diagnostic map and, for each fault, record both the technical response and the safety response.",
        build="A mobile fault diagnostic map with a technical and a safety response for each of ten faults.",
        services="Mobile device, charging cables, battery health screens, device settings",
        steps=[
            ("Record the safety rule that governs this entire lab: a swollen battery must never be punctured, compressed or charged. Power the device down, isolate it from flammable material and follow the organisation's hazardous disposal procedure.", ""),
            ("Check battery health from the device's own settings, recording the maximum capacity percentage and the cycle count where the operating system exposes them.", ""),
            ("Diagnose poor battery life by separating its causes: a genuinely degraded battery, background applications, a weak cellular signal forcing the radio to full transmit power, or high screen brightness.", ""),
            ("Diagnose a swollen battery from its physical signs — a lifting screen, a bulging back cover or a device that no longer sits flat — and apply the safety response immediately.", ""),
            ("Diagnose improper or intermittent charging by testing in a fixed order: substitute the cable, substitute the charger, inspect the port for lint and damage, then suspect the battery.", ""),
            ("Record that a charging port packed with pocket lint is extremely common and is cleared with a wooden or plastic pick on a powered-off device — never a metal tool.", ""),
            ("Diagnose a broken screen by distinguishing cracked glass with a working display, a working digitizer with a dead display, and a working display with a dead digitizer.", ""),
            ("Diagnose digitizer failure as touch that is unresponsive, offset from where you press, or registering phantom touches, while the image remains perfect.", ""),
            ("Diagnose overheating from its causes: sustained high load, a faulty charging circuit, an ambient heat source, or a failing battery — and record that persistent overheating with a swollen battery is an immediate safety stop.", ""),
            ("Diagnose liquid damage, record that the device must be powered off and not charged, and note that visible corrosion means board-level damage requiring specialist repair.", ""),
            ("Diagnose physically damaged ports and record the repair options in order of cost: port replacement by a specialist, board replacement, or device replacement.", ""),
            ("Complete the map with poor or no connectivity, cursor drift requiring touch recalibration, and malware symptoms, giving a technical and a safety response for each.", ""),
        ],
        flow=[
            ("State the safety rule", "Never puncture, compress or charge a swollen battery"),
            ("Read the battery", "Maximum capacity and cycle count from device settings"),
            ("Split the causes", "Degraded cell, background apps, weak signal or brightness"),
            ("Test the charge path", "Cable, then charger, then the port, then the battery"),
            ("Separate screen faults", "Cracked glass, dead display and dead digitizer differ"),
        ],
        test="Every fault in your map has both a technical and a safety response; the swollen battery entry forbids puncturing, compressing and charging; and the charging diagnosis substitutes cable and charger before the battery is suspected.",
    ),
    dict(
        num=27, topic=5,
        title="Printer Fault Diagnosis from Print Defects",
        objective="Diagnose printer faults by reading the physical defect on the page and mapping it to the failed component in the print process (Core 1 objective 5.6).",
        desc="A printed page is diagnostic evidence: the defect's pattern tells you which component failed and often where in the paper path. You build the defect-to-component map, then work through paper handling, connectivity and driver faults that produce no defect at all.",
        build="A print defect diagnostic map covering twelve defects, plus a paper-path and connectivity fault checklist.",
        services="Laser and inkjet printers or reference defect images, maintenance kit reference, print management console",
        steps=[
            ("Record the diagnostic principle: the pattern of the defect identifies the component, and a repeating defect's spacing identifies which rotating part by its circumference.", ""),
            ("Diagnose vertical lines running down every page on a laser printer as a scratched imaging drum, and record that the fix is drum or cartridge replacement.", ""),
            ("Diagnose toner that smudges or wipes off the page as a failed fuser — the toner was placed correctly but never melted onto the paper.", ""),
            ("Diagnose repeated ghost or echo images as a drum that is not being cleaned between cycles, and record that replacing the toner cartridge usually resolves it.", ""),
            ("Diagnose faded print as low toner or ink, and record the field workaround of gently rocking a laser cartridge to redistribute remaining toner.", ""),
            ("Diagnose garbled or nonsense output as the wrong print driver language — a PostScript driver sending to a PCL-only printer, or a corrupt driver.", ""),
            ("Diagnose entirely blank or entirely black pages as a primary corona or charging failure, since the drum is either never charged or never discharged.", ""),
            ("Diagnose paper jams by location: the pickup area points at worn pickup rollers or separation pads, the fuser area points at the fuser or wrong paper weight.", ""),
            ("Diagnose multi-page misfeeds as worn separation pads or paper that is damp, curled or of the wrong weight for the tray.", ""),
            ("Diagnose incorrect colour output on an inkjet by checking cartridge seating, checking for third-party ink, running head cleaning and running a colour calibration.", ""),
            ("Diagnose incorrect paper size or orientation as an application, driver or tray configuration mismatch rather than a hardware fault, and record all three places to check.", ""),
            ("Build the connectivity checklist for a printer that prints nothing at all: check the print queue for stalled jobs, restart the print spooler, verify network reachability and confirm the correct printer is selected.", "net stop spooler && net start spooler"),
        ],
        flow=[
            ("Read the page", "The defect pattern names the component that failed"),
            ("Trace the drum", "Vertical lines and repeating ghost images"),
            ("Trace the fuser", "Toner that wipes off was never melted onto the paper"),
            ("Trace paper handling", "Jams and misfeeds point at rollers and separation pads"),
            ("Clear the queue", "No output at all: spooler, queue, network and driver"),
        ],
        test="Your map covers all twelve defects with a named component and a corrective action; the fuser, drum and corona faults are correctly distinguished; and the connectivity checklist includes clearing the queue and restarting the spooler.",
    ),
    dict(
        num=28, topic=5,
        title="Network Fault Diagnosis — Command Line to Packet Level",
        objective="Diagnose network faults by working the layers in order, from physical connectivity through addressing and DNS to application behaviour (Core 1 objective 5.7).",
        desc="Network faults are diagnosed layer by layer, because testing at the wrong layer wastes time and misleads. You work a fixed bottom-up sequence with a specific command at each layer, then use packet analysis to resolve what the commands alone cannot.",
        build="A layered network diagnostic sequence with a command per layer, and a symptom-to-cause map for eight network faults.",
        services="Killercoda Ubuntu Playground, PCAP Analyzer, IP Calculator, ping, traceroute, dig, ss, tcpdump",
        steps=[
            ("Build the diagnostic sequence bottom-up: physical link, IP addressing, default gateway, DNS resolution, then the application itself. Testing out of order produces misleading results.", ""),
            ("Open the Killercoda Ubuntu playground and install the diagnostic tool set.", "apt-get update -qq && apt-get install -y iproute2 dnsutils traceroute net-tools tcpdump curl"),
            ("Layer 1 — confirm the interface is physically up and has carrier, since every layer above depends on it.", "ip -brief link show"),
            ("Layer 2 and 3 — record the interface addresses and identify whether the address is valid, or an APIPA 169.254 address meaning DHCP failed.", "ip -brief addr show"),
            ("Verify the address and mask in IP Calculator at https://alfredang.github.io/ipcalculator/ and confirm the host sits inside its own subnet's usable range.", ""),
            ("Layer 3 — confirm a default route exists, then test reachability to the gateway. No default route means no traffic can leave the subnet.", "ip route show && ping -c 3 $(ip route | awk '/default/ {print $3}')"),
            ("Test reachability beyond the gateway by IP address, which isolates routing from DNS entirely.", "ping -c 3 8.8.8.8"),
            ("Layer 7 — test DNS resolution separately, because a machine that pings an IP but not a name has a DNS fault and nothing else.", "dig +short www.tertiarycourses.com.sg && cat /etc/resolv.conf"),
            ("Trace the path to identify where latency is introduced or where the path stops.", "traceroute -m 12 8.8.8.8"),
            ("Check active connections and listening sockets to confirm the application layer is actually working.", "ss -tunap | head -20"),
            ("Capture traffic and analyse it in PCAP Analyzer when the commands are inconclusive, since the packets show what the tools only summarise.", "tcpdump -i any -c 100 -w /root/netfault.pcap & sleep 3; curl -s https://example.com > /dev/null; wait; tcpdump -r /root/netfault.pcap -nn | head -20"),
            ("Build the symptom map for APIPA address, no default gateway, pings IP but not name, high latency, jitter degrading VoIP, port flapping, intermittent wireless and limited connectivity — giving the layer, the test and the fix for each.", ""),
        ],
        flow=[
            ("Order the layers", "Link, addressing, gateway, DNS, then the application"),
            ("Prove the link", "ip -brief link show before anything above it"),
            ("Validate the address", "APIPA means DHCP never answered; confirm the subnet"),
            ("Split routing from DNS", "Ping by IP, then by name, and compare the results"),
            ("Capture when stuck", "tcpdump and PCAP Analyzer when commands are inconclusive"),
        ],
        test="Every command in the sequence runs successfully on the playground, the sequence tests layers strictly bottom-up, IP Calculator confirms the host address sits within its usable range, and the symptom map assigns a layer and a specific test to all eight faults.",
    ),
]
